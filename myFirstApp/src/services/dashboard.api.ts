// import api from "./api";

// export const getProfile = async () =>
//   (await api.get("/api/users/me")).data;

// export const getTodayAttendance = async () =>
//   (await api.get("/api/attendance/today")).data;

// export const getAttendanceSummary = async () =>
//   (await api.get("/api/attendance/summary")).data;

// export const punchIn = async () =>
//   (await api.post("/api/attendance/punch-in")).data;
